server:
  port: 8080

spring:
  application:
    name: rest-api-consumer

# Only the registry URL is needed — no agent name, port, or version.
# The app starts in consumer-only mode automatically when @MeshRoute
# is used without @MeshAgent.
mesh:
  registry:
    url: http://localhost:8000

logging:
  level:
    io.mcpmesh: DEBUG
    com.example: DEBUG
