server:
  port: 8080

spring:
  application:
    name: rest-api-consumer

# MCP Mesh configuration
mesh:
  agent:
    name: rest-api-consumer
    version: "1.0.0"
    port: 9010
  registry:
    url: http://localhost:8000

logging:
  level:
    io.mcpmesh: DEBUG
    com.example: DEBUG
