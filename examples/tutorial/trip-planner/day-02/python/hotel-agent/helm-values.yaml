# Helm values for deploying hotel-agent with mcp-mesh-agent chart
# Usage: helm install hotel-agent oci://ghcr.io/dhyansraj/mcp-mesh/mcp-mesh-agent -f helm-values.yaml
# Check latest version: helm show chart oci://ghcr.io/dhyansraj/mcp-mesh/mcp-mesh-agent

image:
  repository: your-registry/hotel-agent
  tag: "v1.0.0"
  pullPolicy: IfNotPresent

agent:
  name: hotel-agent
  runtime: python

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

mesh:
  enabled: true

registry:
  host: "mcp-core-mcp-mesh-registry"
  port: "8000"
