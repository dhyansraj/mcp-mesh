# Self-signed ClusterIssuer for MCP Mesh TLS.
# This is used ONLY to bootstrap the root CA certificate.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: mcp-mesh-selfsigned-issuer
spec:
  selfSigned: {}
