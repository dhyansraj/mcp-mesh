apiVersion: v1
kind: Secret
metadata:
  name: mcp-agent-secret
  namespace: mcp-mesh
type: Opaque
stringData:
  # API keys for external services
  api-key: "your-api-key-here"

  # Service credentials
  service-username: "agent-user"
  service-password: "changeme"

  # Custom secrets for agents
  custom-secret-1: "value1"
  custom-secret-2: "value2"
