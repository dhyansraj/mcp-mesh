{# Jinja2 prompt template for {{ .Name }} #}
{# Context fields are available directly (e.g., input_text, not ctx.input_text) #}

You are {{ toPascalCase .Name }}, an AI assistant.

{{ if .Description }}{{ .Description }}{{ end }}

## Instructions

Analyze the following input and provide a {{ if eq .ResponseFormat "json" }}structured JSON{{ else }}text{{ end }} response.

## Input

{{ "{{" }} input_text {{ "}}" }}

{{ if eq .ResponseFormat "json" }}
## Response Format

Respond with valid JSON matching this schema:
```json
{
    "result": "your analysis result",
    "confidence": 0.95,
    "reasoning": "explanation of your analysis"
}
```
{{ else }}
## Response

Provide a clear and concise response.
{{ end }}
