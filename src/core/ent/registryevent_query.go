// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"mcp-mesh/src/core/ent/agent"
	"mcp-mesh/src/core/ent/predicate"
	"mcp-mesh/src/core/ent/registryevent"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RegistryEventQuery is the builder for querying RegistryEvent entities.
type RegistryEventQuery struct {
	config
	ctx        *QueryContext
	order      []registryevent.OrderOption
	inters     []Interceptor
	predicates []predicate.RegistryEvent
	withAgent  *AgentQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RegistryEventQuery builder.
func (req *RegistryEventQuery) Where(ps ...predicate.RegistryEvent) *RegistryEventQuery {
	req.predicates = append(req.predicates, ps...)
	return req
}

// Limit the number of records to be returned by this query.
func (req *RegistryEventQuery) Limit(limit int) *RegistryEventQuery {
	req.ctx.Limit = &limit
	return req
}

// Offset to start from.
func (req *RegistryEventQuery) Offset(offset int) *RegistryEventQuery {
	req.ctx.Offset = &offset
	return req
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (req *RegistryEventQuery) Unique(unique bool) *RegistryEventQuery {
	req.ctx.Unique = &unique
	return req
}

// Order specifies how the records should be ordered.
func (req *RegistryEventQuery) Order(o ...registryevent.OrderOption) *RegistryEventQuery {
	req.order = append(req.order, o...)
	return req
}

// QueryAgent chains the current query on the "agent" edge.
func (req *RegistryEventQuery) QueryAgent() *AgentQuery {
	query := (&AgentClient{config: req.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := req.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := req.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(registryevent.Table, registryevent.FieldID, selector),
			sqlgraph.To(agent.Table, agent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, registryevent.AgentTable, registryevent.AgentColumn),
		)
		fromU = sqlgraph.SetNeighbors(req.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RegistryEvent entity from the query.
// Returns a *NotFoundError when no RegistryEvent was found.
func (req *RegistryEventQuery) First(ctx context.Context) (*RegistryEvent, error) {
	nodes, err := req.Limit(1).All(setContextOp(ctx, req.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{registryevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (req *RegistryEventQuery) FirstX(ctx context.Context) *RegistryEvent {
	node, err := req.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RegistryEvent ID from the query.
// Returns a *NotFoundError when no RegistryEvent ID was found.
func (req *RegistryEventQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = req.Limit(1).IDs(setContextOp(ctx, req.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{registryevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (req *RegistryEventQuery) FirstIDX(ctx context.Context) int {
	id, err := req.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RegistryEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RegistryEvent entity is found.
// Returns a *NotFoundError when no RegistryEvent entities are found.
func (req *RegistryEventQuery) Only(ctx context.Context) (*RegistryEvent, error) {
	nodes, err := req.Limit(2).All(setContextOp(ctx, req.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{registryevent.Label}
	default:
		return nil, &NotSingularError{registryevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (req *RegistryEventQuery) OnlyX(ctx context.Context) *RegistryEvent {
	node, err := req.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RegistryEvent ID in the query.
// Returns a *NotSingularError when more than one RegistryEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (req *RegistryEventQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = req.Limit(2).IDs(setContextOp(ctx, req.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{registryevent.Label}
	default:
		err = &NotSingularError{registryevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (req *RegistryEventQuery) OnlyIDX(ctx context.Context) int {
	id, err := req.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RegistryEvents.
func (req *RegistryEventQuery) All(ctx context.Context) ([]*RegistryEvent, error) {
	ctx = setContextOp(ctx, req.ctx, ent.OpQueryAll)
	if err := req.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RegistryEvent, *RegistryEventQuery]()
	return withInterceptors[[]*RegistryEvent](ctx, req, qr, req.inters)
}

// AllX is like All, but panics if an error occurs.
func (req *RegistryEventQuery) AllX(ctx context.Context) []*RegistryEvent {
	nodes, err := req.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RegistryEvent IDs.
func (req *RegistryEventQuery) IDs(ctx context.Context) (ids []int, err error) {
	if req.ctx.Unique == nil && req.path != nil {
		req.Unique(true)
	}
	ctx = setContextOp(ctx, req.ctx, ent.OpQueryIDs)
	if err = req.Select(registryevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (req *RegistryEventQuery) IDsX(ctx context.Context) []int {
	ids, err := req.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (req *RegistryEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, req.ctx, ent.OpQueryCount)
	if err := req.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, req, querierCount[*RegistryEventQuery](), req.inters)
}

// CountX is like Count, but panics if an error occurs.
func (req *RegistryEventQuery) CountX(ctx context.Context) int {
	count, err := req.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (req *RegistryEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, req.ctx, ent.OpQueryExist)
	switch _, err := req.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (req *RegistryEventQuery) ExistX(ctx context.Context) bool {
	exist, err := req.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RegistryEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (req *RegistryEventQuery) Clone() *RegistryEventQuery {
	if req == nil {
		return nil
	}
	return &RegistryEventQuery{
		config:     req.config,
		ctx:        req.ctx.Clone(),
		order:      append([]registryevent.OrderOption{}, req.order...),
		inters:     append([]Interceptor{}, req.inters...),
		predicates: append([]predicate.RegistryEvent{}, req.predicates...),
		withAgent:  req.withAgent.Clone(),
		// clone intermediate query.
		sql:  req.sql.Clone(),
		path: req.path,
	}
}

// WithAgent tells the query-builder to eager-load the nodes that are connected to
// the "agent" edge. The optional arguments are used to configure the query builder of the edge.
func (req *RegistryEventQuery) WithAgent(opts ...func(*AgentQuery)) *RegistryEventQuery {
	query := (&AgentClient{config: req.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	req.withAgent = query
	return req
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EventType registryevent.EventType `json:"event_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RegistryEvent.Query().
//		GroupBy(registryevent.FieldEventType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (req *RegistryEventQuery) GroupBy(field string, fields ...string) *RegistryEventGroupBy {
	req.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RegistryEventGroupBy{build: req}
	grbuild.flds = &req.ctx.Fields
	grbuild.label = registryevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EventType registryevent.EventType `json:"event_type,omitempty"`
//	}
//
//	client.RegistryEvent.Query().
//		Select(registryevent.FieldEventType).
//		Scan(ctx, &v)
func (req *RegistryEventQuery) Select(fields ...string) *RegistryEventSelect {
	req.ctx.Fields = append(req.ctx.Fields, fields...)
	sbuild := &RegistryEventSelect{RegistryEventQuery: req}
	sbuild.label = registryevent.Label
	sbuild.flds, sbuild.scan = &req.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RegistryEventSelect configured with the given aggregations.
func (req *RegistryEventQuery) Aggregate(fns ...AggregateFunc) *RegistryEventSelect {
	return req.Select().Aggregate(fns...)
}

func (req *RegistryEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range req.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, req); err != nil {
				return err
			}
		}
	}
	for _, f := range req.ctx.Fields {
		if !registryevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if req.path != nil {
		prev, err := req.path(ctx)
		if err != nil {
			return err
		}
		req.sql = prev
	}
	return nil
}

func (req *RegistryEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RegistryEvent, error) {
	var (
		nodes       = []*RegistryEvent{}
		withFKs     = req.withFKs
		_spec       = req.querySpec()
		loadedTypes = [1]bool{
			req.withAgent != nil,
		}
	)
	if req.withAgent != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, registryevent.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RegistryEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RegistryEvent{config: req.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, req.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := req.withAgent; query != nil {
		if err := req.loadAgent(ctx, query, nodes, nil,
			func(n *RegistryEvent, e *Agent) { n.Edges.Agent = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (req *RegistryEventQuery) loadAgent(ctx context.Context, query *AgentQuery, nodes []*RegistryEvent, init func(*RegistryEvent), assign func(*RegistryEvent, *Agent)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*RegistryEvent)
	for i := range nodes {
		if nodes[i].agent_events == nil {
			continue
		}
		fk := *nodes[i].agent_events
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(agent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "agent_events" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (req *RegistryEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := req.querySpec()
	_spec.Node.Columns = req.ctx.Fields
	if len(req.ctx.Fields) > 0 {
		_spec.Unique = req.ctx.Unique != nil && *req.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, req.driver, _spec)
}

func (req *RegistryEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(registryevent.Table, registryevent.Columns, sqlgraph.NewFieldSpec(registryevent.FieldID, field.TypeInt))
	_spec.From = req.sql
	if unique := req.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if req.path != nil {
		_spec.Unique = true
	}
	if fields := req.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, registryevent.FieldID)
		for i := range fields {
			if fields[i] != registryevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := req.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := req.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := req.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := req.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (req *RegistryEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(req.driver.Dialect())
	t1 := builder.Table(registryevent.Table)
	columns := req.ctx.Fields
	if len(columns) == 0 {
		columns = registryevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if req.sql != nil {
		selector = req.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if req.ctx.Unique != nil && *req.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range req.predicates {
		p(selector)
	}
	for _, p := range req.order {
		p(selector)
	}
	if offset := req.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := req.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RegistryEventGroupBy is the group-by builder for RegistryEvent entities.
type RegistryEventGroupBy struct {
	selector
	build *RegistryEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (regb *RegistryEventGroupBy) Aggregate(fns ...AggregateFunc) *RegistryEventGroupBy {
	regb.fns = append(regb.fns, fns...)
	return regb
}

// Scan applies the selector query and scans the result into the given value.
func (regb *RegistryEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, regb.build.ctx, ent.OpQueryGroupBy)
	if err := regb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RegistryEventQuery, *RegistryEventGroupBy](ctx, regb.build, regb, regb.build.inters, v)
}

func (regb *RegistryEventGroupBy) sqlScan(ctx context.Context, root *RegistryEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(regb.fns))
	for _, fn := range regb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*regb.flds)+len(regb.fns))
		for _, f := range *regb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*regb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := regb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RegistryEventSelect is the builder for selecting fields of RegistryEvent entities.
type RegistryEventSelect struct {
	*RegistryEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (res *RegistryEventSelect) Aggregate(fns ...AggregateFunc) *RegistryEventSelect {
	res.fns = append(res.fns, fns...)
	return res
}

// Scan applies the selector query and scans the result into the given value.
func (res *RegistryEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, res.ctx, ent.OpQuerySelect)
	if err := res.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RegistryEventQuery, *RegistryEventSelect](ctx, res.RegistryEventQuery, res, res.inters, v)
}

func (res *RegistryEventSelect) sqlScan(ctx context.Context, root *RegistryEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(res.fns))
	for _, fn := range res.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*res.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := res.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
