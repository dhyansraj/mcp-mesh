// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"mcp-mesh/src/core/ent/agent"
	"mcp-mesh/src/core/ent/llmtoolresolution"
	"mcp-mesh/src/core/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// LLMToolResolutionQuery is the builder for querying LLMToolResolution entities.
type LLMToolResolutionQuery struct {
	config
	ctx               *QueryContext
	order             []llmtoolresolution.OrderOption
	inters            []Interceptor
	predicates        []predicate.LLMToolResolution
	withConsumerAgent *AgentQuery
	withProviderAgent *AgentQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LLMToolResolutionQuery builder.
func (ltrq *LLMToolResolutionQuery) Where(ps ...predicate.LLMToolResolution) *LLMToolResolutionQuery {
	ltrq.predicates = append(ltrq.predicates, ps...)
	return ltrq
}

// Limit the number of records to be returned by this query.
func (ltrq *LLMToolResolutionQuery) Limit(limit int) *LLMToolResolutionQuery {
	ltrq.ctx.Limit = &limit
	return ltrq
}

// Offset to start from.
func (ltrq *LLMToolResolutionQuery) Offset(offset int) *LLMToolResolutionQuery {
	ltrq.ctx.Offset = &offset
	return ltrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ltrq *LLMToolResolutionQuery) Unique(unique bool) *LLMToolResolutionQuery {
	ltrq.ctx.Unique = &unique
	return ltrq
}

// Order specifies how the records should be ordered.
func (ltrq *LLMToolResolutionQuery) Order(o ...llmtoolresolution.OrderOption) *LLMToolResolutionQuery {
	ltrq.order = append(ltrq.order, o...)
	return ltrq
}

// QueryConsumerAgent chains the current query on the "consumer_agent" edge.
func (ltrq *LLMToolResolutionQuery) QueryConsumerAgent() *AgentQuery {
	query := (&AgentClient{config: ltrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ltrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ltrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(llmtoolresolution.Table, llmtoolresolution.FieldID, selector),
			sqlgraph.To(agent.Table, agent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, llmtoolresolution.ConsumerAgentTable, llmtoolresolution.ConsumerAgentColumn),
		)
		fromU = sqlgraph.SetNeighbors(ltrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProviderAgent chains the current query on the "provider_agent" edge.
func (ltrq *LLMToolResolutionQuery) QueryProviderAgent() *AgentQuery {
	query := (&AgentClient{config: ltrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ltrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ltrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(llmtoolresolution.Table, llmtoolresolution.FieldID, selector),
			sqlgraph.To(agent.Table, agent.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, llmtoolresolution.ProviderAgentTable, llmtoolresolution.ProviderAgentColumn),
		)
		fromU = sqlgraph.SetNeighbors(ltrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LLMToolResolution entity from the query.
// Returns a *NotFoundError when no LLMToolResolution was found.
func (ltrq *LLMToolResolutionQuery) First(ctx context.Context) (*LLMToolResolution, error) {
	nodes, err := ltrq.Limit(1).All(setContextOp(ctx, ltrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{llmtoolresolution.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ltrq *LLMToolResolutionQuery) FirstX(ctx context.Context) *LLMToolResolution {
	node, err := ltrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LLMToolResolution ID from the query.
// Returns a *NotFoundError when no LLMToolResolution ID was found.
func (ltrq *LLMToolResolutionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ltrq.Limit(1).IDs(setContextOp(ctx, ltrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{llmtoolresolution.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ltrq *LLMToolResolutionQuery) FirstIDX(ctx context.Context) int {
	id, err := ltrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LLMToolResolution entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LLMToolResolution entity is found.
// Returns a *NotFoundError when no LLMToolResolution entities are found.
func (ltrq *LLMToolResolutionQuery) Only(ctx context.Context) (*LLMToolResolution, error) {
	nodes, err := ltrq.Limit(2).All(setContextOp(ctx, ltrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{llmtoolresolution.Label}
	default:
		return nil, &NotSingularError{llmtoolresolution.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ltrq *LLMToolResolutionQuery) OnlyX(ctx context.Context) *LLMToolResolution {
	node, err := ltrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LLMToolResolution ID in the query.
// Returns a *NotSingularError when more than one LLMToolResolution ID is found.
// Returns a *NotFoundError when no entities are found.
func (ltrq *LLMToolResolutionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ltrq.Limit(2).IDs(setContextOp(ctx, ltrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{llmtoolresolution.Label}
	default:
		err = &NotSingularError{llmtoolresolution.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ltrq *LLMToolResolutionQuery) OnlyIDX(ctx context.Context) int {
	id, err := ltrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LLMToolResolutions.
func (ltrq *LLMToolResolutionQuery) All(ctx context.Context) ([]*LLMToolResolution, error) {
	ctx = setContextOp(ctx, ltrq.ctx, ent.OpQueryAll)
	if err := ltrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LLMToolResolution, *LLMToolResolutionQuery]()
	return withInterceptors[[]*LLMToolResolution](ctx, ltrq, qr, ltrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ltrq *LLMToolResolutionQuery) AllX(ctx context.Context) []*LLMToolResolution {
	nodes, err := ltrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LLMToolResolution IDs.
func (ltrq *LLMToolResolutionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ltrq.ctx.Unique == nil && ltrq.path != nil {
		ltrq.Unique(true)
	}
	ctx = setContextOp(ctx, ltrq.ctx, ent.OpQueryIDs)
	if err = ltrq.Select(llmtoolresolution.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ltrq *LLMToolResolutionQuery) IDsX(ctx context.Context) []int {
	ids, err := ltrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ltrq *LLMToolResolutionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ltrq.ctx, ent.OpQueryCount)
	if err := ltrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ltrq, querierCount[*LLMToolResolutionQuery](), ltrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ltrq *LLMToolResolutionQuery) CountX(ctx context.Context) int {
	count, err := ltrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ltrq *LLMToolResolutionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ltrq.ctx, ent.OpQueryExist)
	switch _, err := ltrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ltrq *LLMToolResolutionQuery) ExistX(ctx context.Context) bool {
	exist, err := ltrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LLMToolResolutionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ltrq *LLMToolResolutionQuery) Clone() *LLMToolResolutionQuery {
	if ltrq == nil {
		return nil
	}
	return &LLMToolResolutionQuery{
		config:            ltrq.config,
		ctx:               ltrq.ctx.Clone(),
		order:             append([]llmtoolresolution.OrderOption{}, ltrq.order...),
		inters:            append([]Interceptor{}, ltrq.inters...),
		predicates:        append([]predicate.LLMToolResolution{}, ltrq.predicates...),
		withConsumerAgent: ltrq.withConsumerAgent.Clone(),
		withProviderAgent: ltrq.withProviderAgent.Clone(),
		// clone intermediate query.
		sql:  ltrq.sql.Clone(),
		path: ltrq.path,
	}
}

// WithConsumerAgent tells the query-builder to eager-load the nodes that are connected to
// the "consumer_agent" edge. The optional arguments are used to configure the query builder of the edge.
func (ltrq *LLMToolResolutionQuery) WithConsumerAgent(opts ...func(*AgentQuery)) *LLMToolResolutionQuery {
	query := (&AgentClient{config: ltrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ltrq.withConsumerAgent = query
	return ltrq
}

// WithProviderAgent tells the query-builder to eager-load the nodes that are connected to
// the "provider_agent" edge. The optional arguments are used to configure the query builder of the edge.
func (ltrq *LLMToolResolutionQuery) WithProviderAgent(opts ...func(*AgentQuery)) *LLMToolResolutionQuery {
	query := (&AgentClient{config: ltrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ltrq.withProviderAgent = query
	return ltrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ConsumerAgentID string `json:"consumer_agent_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LLMToolResolution.Query().
//		GroupBy(llmtoolresolution.FieldConsumerAgentID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ltrq *LLMToolResolutionQuery) GroupBy(field string, fields ...string) *LLMToolResolutionGroupBy {
	ltrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LLMToolResolutionGroupBy{build: ltrq}
	grbuild.flds = &ltrq.ctx.Fields
	grbuild.label = llmtoolresolution.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ConsumerAgentID string `json:"consumer_agent_id,omitempty"`
//	}
//
//	client.LLMToolResolution.Query().
//		Select(llmtoolresolution.FieldConsumerAgentID).
//		Scan(ctx, &v)
func (ltrq *LLMToolResolutionQuery) Select(fields ...string) *LLMToolResolutionSelect {
	ltrq.ctx.Fields = append(ltrq.ctx.Fields, fields...)
	sbuild := &LLMToolResolutionSelect{LLMToolResolutionQuery: ltrq}
	sbuild.label = llmtoolresolution.Label
	sbuild.flds, sbuild.scan = &ltrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LLMToolResolutionSelect configured with the given aggregations.
func (ltrq *LLMToolResolutionQuery) Aggregate(fns ...AggregateFunc) *LLMToolResolutionSelect {
	return ltrq.Select().Aggregate(fns...)
}

func (ltrq *LLMToolResolutionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ltrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ltrq); err != nil {
				return err
			}
		}
	}
	for _, f := range ltrq.ctx.Fields {
		if !llmtoolresolution.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ltrq.path != nil {
		prev, err := ltrq.path(ctx)
		if err != nil {
			return err
		}
		ltrq.sql = prev
	}
	return nil
}

func (ltrq *LLMToolResolutionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LLMToolResolution, error) {
	var (
		nodes       = []*LLMToolResolution{}
		_spec       = ltrq.querySpec()
		loadedTypes = [2]bool{
			ltrq.withConsumerAgent != nil,
			ltrq.withProviderAgent != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LLMToolResolution).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LLMToolResolution{config: ltrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ltrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ltrq.withConsumerAgent; query != nil {
		if err := ltrq.loadConsumerAgent(ctx, query, nodes, nil,
			func(n *LLMToolResolution, e *Agent) { n.Edges.ConsumerAgent = e }); err != nil {
			return nil, err
		}
	}
	if query := ltrq.withProviderAgent; query != nil {
		if err := ltrq.loadProviderAgent(ctx, query, nodes, nil,
			func(n *LLMToolResolution, e *Agent) { n.Edges.ProviderAgent = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ltrq *LLMToolResolutionQuery) loadConsumerAgent(ctx context.Context, query *AgentQuery, nodes []*LLMToolResolution, init func(*LLMToolResolution), assign func(*LLMToolResolution, *Agent)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*LLMToolResolution)
	for i := range nodes {
		fk := nodes[i].ConsumerAgentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(agent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "consumer_agent_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ltrq *LLMToolResolutionQuery) loadProviderAgent(ctx context.Context, query *AgentQuery, nodes []*LLMToolResolution, init func(*LLMToolResolution), assign func(*LLMToolResolution, *Agent)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*LLMToolResolution)
	for i := range nodes {
		if nodes[i].ProviderAgentID == nil {
			continue
		}
		fk := *nodes[i].ProviderAgentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(agent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "provider_agent_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ltrq *LLMToolResolutionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ltrq.querySpec()
	_spec.Node.Columns = ltrq.ctx.Fields
	if len(ltrq.ctx.Fields) > 0 {
		_spec.Unique = ltrq.ctx.Unique != nil && *ltrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ltrq.driver, _spec)
}

func (ltrq *LLMToolResolutionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(llmtoolresolution.Table, llmtoolresolution.Columns, sqlgraph.NewFieldSpec(llmtoolresolution.FieldID, field.TypeInt))
	_spec.From = ltrq.sql
	if unique := ltrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ltrq.path != nil {
		_spec.Unique = true
	}
	if fields := ltrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, llmtoolresolution.FieldID)
		for i := range fields {
			if fields[i] != llmtoolresolution.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ltrq.withConsumerAgent != nil {
			_spec.Node.AddColumnOnce(llmtoolresolution.FieldConsumerAgentID)
		}
		if ltrq.withProviderAgent != nil {
			_spec.Node.AddColumnOnce(llmtoolresolution.FieldProviderAgentID)
		}
	}
	if ps := ltrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ltrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ltrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ltrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ltrq *LLMToolResolutionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ltrq.driver.Dialect())
	t1 := builder.Table(llmtoolresolution.Table)
	columns := ltrq.ctx.Fields
	if len(columns) == 0 {
		columns = llmtoolresolution.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ltrq.sql != nil {
		selector = ltrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ltrq.ctx.Unique != nil && *ltrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ltrq.predicates {
		p(selector)
	}
	for _, p := range ltrq.order {
		p(selector)
	}
	if offset := ltrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ltrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LLMToolResolutionGroupBy is the group-by builder for LLMToolResolution entities.
type LLMToolResolutionGroupBy struct {
	selector
	build *LLMToolResolutionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ltrgb *LLMToolResolutionGroupBy) Aggregate(fns ...AggregateFunc) *LLMToolResolutionGroupBy {
	ltrgb.fns = append(ltrgb.fns, fns...)
	return ltrgb
}

// Scan applies the selector query and scans the result into the given value.
func (ltrgb *LLMToolResolutionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ltrgb.build.ctx, ent.OpQueryGroupBy)
	if err := ltrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LLMToolResolutionQuery, *LLMToolResolutionGroupBy](ctx, ltrgb.build, ltrgb, ltrgb.build.inters, v)
}

func (ltrgb *LLMToolResolutionGroupBy) sqlScan(ctx context.Context, root *LLMToolResolutionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ltrgb.fns))
	for _, fn := range ltrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ltrgb.flds)+len(ltrgb.fns))
		for _, f := range *ltrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ltrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ltrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LLMToolResolutionSelect is the builder for selecting fields of LLMToolResolution entities.
type LLMToolResolutionSelect struct {
	*LLMToolResolutionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ltrs *LLMToolResolutionSelect) Aggregate(fns ...AggregateFunc) *LLMToolResolutionSelect {
	ltrs.fns = append(ltrs.fns, fns...)
	return ltrs
}

// Scan applies the selector query and scans the result into the given value.
func (ltrs *LLMToolResolutionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ltrs.ctx, ent.OpQuerySelect)
	if err := ltrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LLMToolResolutionQuery, *LLMToolResolutionSelect](ctx, ltrs.LLMToolResolutionQuery, ltrs, ltrs.inters, v)
}

func (ltrs *LLMToolResolutionSelect) sqlScan(ctx context.Context, root *LLMToolResolutionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ltrs.fns))
	for _, fn := range ltrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ltrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ltrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
