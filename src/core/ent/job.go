// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"mcp-mesh/src/core/ent/job"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Job is the model entity for the Job schema.
type Job struct {
	config `json:"-"`
	// ID of the ent.
	// Unique identifier for the job (server-generated UUID)
	ID string `json:"id,omitempty"`
	// Capability name this job targets (e.g. 'render_report')
	Capability string `json:"capability,omitempty"`
	// Pins job to a specific replica instance; NULL when unclaimed/orphaned and eligible for re-claim
	OwnerInstanceID *string `json:"owner_instance_id,omitempty"`
	// Current job status
	Status job.Status `json:"status,omitempty"`
	// Optional progress fraction in [0.0, 1.0]
	Progress *float64 `json:"progress,omitempty"`
	// Optional human-readable progress message
	ProgressMessage *string `json:"progress_message,omitempty"`
	// Terminal result payload (set on status=completed)
	Result map[string]interface{} `json:"result,omitempty"`
	// Error message (set on status=failed)
	Error *string `json:"error,omitempty"`
	// Full submitted args + headers, used for retry replay
	SubmittedPayload map[string]interface{} `json:"submitted_payload,omitempty"`
	// Number of attempts made so far
	AttemptCount int `json:"attempt_count,omitempty"`
	// Maximum number of attempts allowed
	MaxRetries int `json:"max_retries,omitempty"`
	// Per-attempt soft timeout in seconds (NULL = runtime default)
	MaxDuration *int `json:"max_duration,omitempty"`
	// Wall-clock deadline across all retries (NULL = unlimited)
	TotalDeadline *time.Time `json:"total_deadline,omitempty"`
	// When the current owner's lease expires; orphan reclaim eligible after this
	LeaseExpiresAt *time.Time `json:"lease_expires_at,omitempty"`
	// Timestamp of the most recent heartbeat from the owner
	LastHeartbeatAt *time.Time `json:"last_heartbeat_at,omitempty"`
	// When the job was submitted
	SubmittedAt time.Time `json:"submitted_at,omitempty"`
	// Identity of the submitter (entity ID from TLS, if available)
	SubmittedBy  *string `json:"submitted_by,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Job) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case job.FieldResult, job.FieldSubmittedPayload:
			values[i] = new([]byte)
		case job.FieldProgress:
			values[i] = new(sql.NullFloat64)
		case job.FieldAttemptCount, job.FieldMaxRetries, job.FieldMaxDuration:
			values[i] = new(sql.NullInt64)
		case job.FieldID, job.FieldCapability, job.FieldOwnerInstanceID, job.FieldStatus, job.FieldProgressMessage, job.FieldError, job.FieldSubmittedBy:
			values[i] = new(sql.NullString)
		case job.FieldTotalDeadline, job.FieldLeaseExpiresAt, job.FieldLastHeartbeatAt, job.FieldSubmittedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Job fields.
func (j *Job) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case job.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				j.ID = value.String
			}
		case job.FieldCapability:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field capability", values[i])
			} else if value.Valid {
				j.Capability = value.String
			}
		case job.FieldOwnerInstanceID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field owner_instance_id", values[i])
			} else if value.Valid {
				j.OwnerInstanceID = new(string)
				*j.OwnerInstanceID = value.String
			}
		case job.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				j.Status = job.Status(value.String)
			}
		case job.FieldProgress:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field progress", values[i])
			} else if value.Valid {
				j.Progress = new(float64)
				*j.Progress = value.Float64
			}
		case job.FieldProgressMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field progress_message", values[i])
			} else if value.Valid {
				j.ProgressMessage = new(string)
				*j.ProgressMessage = value.String
			}
		case job.FieldResult:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field result", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &j.Result); err != nil {
					return fmt.Errorf("unmarshal field result: %w", err)
				}
			}
		case job.FieldError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error", values[i])
			} else if value.Valid {
				j.Error = new(string)
				*j.Error = value.String
			}
		case job.FieldSubmittedPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field submitted_payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &j.SubmittedPayload); err != nil {
					return fmt.Errorf("unmarshal field submitted_payload: %w", err)
				}
			}
		case job.FieldAttemptCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field attempt_count", values[i])
			} else if value.Valid {
				j.AttemptCount = int(value.Int64)
			}
		case job.FieldMaxRetries:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_retries", values[i])
			} else if value.Valid {
				j.MaxRetries = int(value.Int64)
			}
		case job.FieldMaxDuration:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_duration", values[i])
			} else if value.Valid {
				j.MaxDuration = new(int)
				*j.MaxDuration = int(value.Int64)
			}
		case job.FieldTotalDeadline:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field total_deadline", values[i])
			} else if value.Valid {
				j.TotalDeadline = new(time.Time)
				*j.TotalDeadline = value.Time
			}
		case job.FieldLeaseExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field lease_expires_at", values[i])
			} else if value.Valid {
				j.LeaseExpiresAt = new(time.Time)
				*j.LeaseExpiresAt = value.Time
			}
		case job.FieldLastHeartbeatAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_heartbeat_at", values[i])
			} else if value.Valid {
				j.LastHeartbeatAt = new(time.Time)
				*j.LastHeartbeatAt = value.Time
			}
		case job.FieldSubmittedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field submitted_at", values[i])
			} else if value.Valid {
				j.SubmittedAt = value.Time
			}
		case job.FieldSubmittedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field submitted_by", values[i])
			} else if value.Valid {
				j.SubmittedBy = new(string)
				*j.SubmittedBy = value.String
			}
		default:
			j.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Job.
// This includes values selected through modifiers, order, etc.
func (j *Job) Value(name string) (ent.Value, error) {
	return j.selectValues.Get(name)
}

// Update returns a builder for updating this Job.
// Note that you need to call Job.Unwrap() before calling this method if this Job
// was returned from a transaction, and the transaction was committed or rolled back.
func (j *Job) Update() *JobUpdateOne {
	return NewJobClient(j.config).UpdateOne(j)
}

// Unwrap unwraps the Job entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (j *Job) Unwrap() *Job {
	_tx, ok := j.config.driver.(*txDriver)
	if !ok {
		panic("ent: Job is not a transactional entity")
	}
	j.config.driver = _tx.drv
	return j
}

// String implements the fmt.Stringer.
func (j *Job) String() string {
	var builder strings.Builder
	builder.WriteString("Job(")
	builder.WriteString(fmt.Sprintf("id=%v, ", j.ID))
	builder.WriteString("capability=")
	builder.WriteString(j.Capability)
	builder.WriteString(", ")
	if v := j.OwnerInstanceID; v != nil {
		builder.WriteString("owner_instance_id=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", j.Status))
	builder.WriteString(", ")
	if v := j.Progress; v != nil {
		builder.WriteString("progress=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := j.ProgressMessage; v != nil {
		builder.WriteString("progress_message=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("result=")
	builder.WriteString(fmt.Sprintf("%v", j.Result))
	builder.WriteString(", ")
	if v := j.Error; v != nil {
		builder.WriteString("error=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("submitted_payload=")
	builder.WriteString(fmt.Sprintf("%v", j.SubmittedPayload))
	builder.WriteString(", ")
	builder.WriteString("attempt_count=")
	builder.WriteString(fmt.Sprintf("%v", j.AttemptCount))
	builder.WriteString(", ")
	builder.WriteString("max_retries=")
	builder.WriteString(fmt.Sprintf("%v", j.MaxRetries))
	builder.WriteString(", ")
	if v := j.MaxDuration; v != nil {
		builder.WriteString("max_duration=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := j.TotalDeadline; v != nil {
		builder.WriteString("total_deadline=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := j.LeaseExpiresAt; v != nil {
		builder.WriteString("lease_expires_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := j.LastHeartbeatAt; v != nil {
		builder.WriteString("last_heartbeat_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("submitted_at=")
	builder.WriteString(j.SubmittedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := j.SubmittedBy; v != nil {
		builder.WriteString("submitted_by=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// Jobs is a parsable slice of Job.
type Jobs []*Job
