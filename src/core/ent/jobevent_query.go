// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"mcp-mesh/src/core/ent/jobevent"
	"mcp-mesh/src/core/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// JobEventQuery is the builder for querying JobEvent entities.
type JobEventQuery struct {
	config
	ctx        *QueryContext
	order      []jobevent.OrderOption
	inters     []Interceptor
	predicates []predicate.JobEvent
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JobEventQuery builder.
func (jeq *JobEventQuery) Where(ps ...predicate.JobEvent) *JobEventQuery {
	jeq.predicates = append(jeq.predicates, ps...)
	return jeq
}

// Limit the number of records to be returned by this query.
func (jeq *JobEventQuery) Limit(limit int) *JobEventQuery {
	jeq.ctx.Limit = &limit
	return jeq
}

// Offset to start from.
func (jeq *JobEventQuery) Offset(offset int) *JobEventQuery {
	jeq.ctx.Offset = &offset
	return jeq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jeq *JobEventQuery) Unique(unique bool) *JobEventQuery {
	jeq.ctx.Unique = &unique
	return jeq
}

// Order specifies how the records should be ordered.
func (jeq *JobEventQuery) Order(o ...jobevent.OrderOption) *JobEventQuery {
	jeq.order = append(jeq.order, o...)
	return jeq
}

// First returns the first JobEvent entity from the query.
// Returns a *NotFoundError when no JobEvent was found.
func (jeq *JobEventQuery) First(ctx context.Context) (*JobEvent, error) {
	nodes, err := jeq.Limit(1).All(setContextOp(ctx, jeq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jobevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jeq *JobEventQuery) FirstX(ctx context.Context) *JobEvent {
	node, err := jeq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JobEvent ID from the query.
// Returns a *NotFoundError when no JobEvent ID was found.
func (jeq *JobEventQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jeq.Limit(1).IDs(setContextOp(ctx, jeq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jobevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jeq *JobEventQuery) FirstIDX(ctx context.Context) int {
	id, err := jeq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JobEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JobEvent entity is found.
// Returns a *NotFoundError when no JobEvent entities are found.
func (jeq *JobEventQuery) Only(ctx context.Context) (*JobEvent, error) {
	nodes, err := jeq.Limit(2).All(setContextOp(ctx, jeq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jobevent.Label}
	default:
		return nil, &NotSingularError{jobevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jeq *JobEventQuery) OnlyX(ctx context.Context) *JobEvent {
	node, err := jeq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JobEvent ID in the query.
// Returns a *NotSingularError when more than one JobEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (jeq *JobEventQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jeq.Limit(2).IDs(setContextOp(ctx, jeq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jobevent.Label}
	default:
		err = &NotSingularError{jobevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jeq *JobEventQuery) OnlyIDX(ctx context.Context) int {
	id, err := jeq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JobEvents.
func (jeq *JobEventQuery) All(ctx context.Context) ([]*JobEvent, error) {
	ctx = setContextOp(ctx, jeq.ctx, ent.OpQueryAll)
	if err := jeq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*JobEvent, *JobEventQuery]()
	return withInterceptors[[]*JobEvent](ctx, jeq, qr, jeq.inters)
}

// AllX is like All, but panics if an error occurs.
func (jeq *JobEventQuery) AllX(ctx context.Context) []*JobEvent {
	nodes, err := jeq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JobEvent IDs.
func (jeq *JobEventQuery) IDs(ctx context.Context) (ids []int, err error) {
	if jeq.ctx.Unique == nil && jeq.path != nil {
		jeq.Unique(true)
	}
	ctx = setContextOp(ctx, jeq.ctx, ent.OpQueryIDs)
	if err = jeq.Select(jobevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jeq *JobEventQuery) IDsX(ctx context.Context) []int {
	ids, err := jeq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jeq *JobEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, jeq.ctx, ent.OpQueryCount)
	if err := jeq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, jeq, querierCount[*JobEventQuery](), jeq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (jeq *JobEventQuery) CountX(ctx context.Context) int {
	count, err := jeq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jeq *JobEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, jeq.ctx, ent.OpQueryExist)
	switch _, err := jeq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (jeq *JobEventQuery) ExistX(ctx context.Context) bool {
	exist, err := jeq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JobEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jeq *JobEventQuery) Clone() *JobEventQuery {
	if jeq == nil {
		return nil
	}
	return &JobEventQuery{
		config:     jeq.config,
		ctx:        jeq.ctx.Clone(),
		order:      append([]jobevent.OrderOption{}, jeq.order...),
		inters:     append([]Interceptor{}, jeq.inters...),
		predicates: append([]predicate.JobEvent{}, jeq.predicates...),
		// clone intermediate query.
		sql:  jeq.sql.Clone(),
		path: jeq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Seq int64 `json:"seq,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JobEvent.Query().
//		GroupBy(jobevent.FieldSeq).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (jeq *JobEventQuery) GroupBy(field string, fields ...string) *JobEventGroupBy {
	jeq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JobEventGroupBy{build: jeq}
	grbuild.flds = &jeq.ctx.Fields
	grbuild.label = jobevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Seq int64 `json:"seq,omitempty"`
//	}
//
//	client.JobEvent.Query().
//		Select(jobevent.FieldSeq).
//		Scan(ctx, &v)
func (jeq *JobEventQuery) Select(fields ...string) *JobEventSelect {
	jeq.ctx.Fields = append(jeq.ctx.Fields, fields...)
	sbuild := &JobEventSelect{JobEventQuery: jeq}
	sbuild.label = jobevent.Label
	sbuild.flds, sbuild.scan = &jeq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JobEventSelect configured with the given aggregations.
func (jeq *JobEventQuery) Aggregate(fns ...AggregateFunc) *JobEventSelect {
	return jeq.Select().Aggregate(fns...)
}

func (jeq *JobEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range jeq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, jeq); err != nil {
				return err
			}
		}
	}
	for _, f := range jeq.ctx.Fields {
		if !jobevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if jeq.path != nil {
		prev, err := jeq.path(ctx)
		if err != nil {
			return err
		}
		jeq.sql = prev
	}
	return nil
}

func (jeq *JobEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JobEvent, error) {
	var (
		nodes = []*JobEvent{}
		_spec = jeq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JobEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JobEvent{config: jeq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jeq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (jeq *JobEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jeq.querySpec()
	_spec.Node.Columns = jeq.ctx.Fields
	if len(jeq.ctx.Fields) > 0 {
		_spec.Unique = jeq.ctx.Unique != nil && *jeq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, jeq.driver, _spec)
}

func (jeq *JobEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jobevent.Table, jobevent.Columns, sqlgraph.NewFieldSpec(jobevent.FieldID, field.TypeInt))
	_spec.From = jeq.sql
	if unique := jeq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if jeq.path != nil {
		_spec.Unique = true
	}
	if fields := jeq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jobevent.FieldID)
		for i := range fields {
			if fields[i] != jobevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := jeq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jeq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jeq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jeq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jeq *JobEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jeq.driver.Dialect())
	t1 := builder.Table(jobevent.Table)
	columns := jeq.ctx.Fields
	if len(columns) == 0 {
		columns = jobevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jeq.sql != nil {
		selector = jeq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jeq.ctx.Unique != nil && *jeq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range jeq.predicates {
		p(selector)
	}
	for _, p := range jeq.order {
		p(selector)
	}
	if offset := jeq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jeq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JobEventGroupBy is the group-by builder for JobEvent entities.
type JobEventGroupBy struct {
	selector
	build *JobEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jegb *JobEventGroupBy) Aggregate(fns ...AggregateFunc) *JobEventGroupBy {
	jegb.fns = append(jegb.fns, fns...)
	return jegb
}

// Scan applies the selector query and scans the result into the given value.
func (jegb *JobEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jegb.build.ctx, ent.OpQueryGroupBy)
	if err := jegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobEventQuery, *JobEventGroupBy](ctx, jegb.build, jegb, jegb.build.inters, v)
}

func (jegb *JobEventGroupBy) sqlScan(ctx context.Context, root *JobEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(jegb.fns))
	for _, fn := range jegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*jegb.flds)+len(jegb.fns))
		for _, f := range *jegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*jegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JobEventSelect is the builder for selecting fields of JobEvent entities.
type JobEventSelect struct {
	*JobEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (jes *JobEventSelect) Aggregate(fns ...AggregateFunc) *JobEventSelect {
	jes.fns = append(jes.fns, fns...)
	return jes
}

// Scan applies the selector query and scans the result into the given value.
func (jes *JobEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jes.ctx, ent.OpQuerySelect)
	if err := jes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobEventQuery, *JobEventSelect](ctx, jes.JobEventQuery, jes, jes.inters, v)
}

func (jes *JobEventSelect) sqlScan(ctx context.Context, root *JobEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(jes.fns))
	for _, fn := range jes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*jes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
