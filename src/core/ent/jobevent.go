// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"mcp-mesh/src/core/ent/jobevent"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// JobEvent is the model entity for the JobEvent schema.
type JobEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Per-job monotonic sequence number, assigned by registry on POST
	Seq int64 `json:"seq,omitempty"`
	// ID of the job this event belongs to
	JobID string `json:"job_id,omitempty"`
	// Event type tag (e.g. 'extend_deadline', 'cancelled', user-defined)
	Type string `json:"type,omitempty"`
	// Arbitrary JSON event payload
	Payload json.RawMessage `json:"payload,omitempty"`
	// W3C trace context (traceparent + tracestate) propagated from sender
	TraceContext map[string]interface{} `json:"trace_context,omitempty"`
	// Identity of the sender (entity ID from TLS, if available)
	PostedBy *string `json:"posted_by,omitempty"`
	// Server-assigned timestamp
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JobEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case jobevent.FieldPayload, jobevent.FieldTraceContext:
			values[i] = new([]byte)
		case jobevent.FieldID, jobevent.FieldSeq:
			values[i] = new(sql.NullInt64)
		case jobevent.FieldJobID, jobevent.FieldType, jobevent.FieldPostedBy:
			values[i] = new(sql.NullString)
		case jobevent.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JobEvent fields.
func (je *JobEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case jobevent.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			je.ID = int(value.Int64)
		case jobevent.FieldSeq:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field seq", values[i])
			} else if value.Valid {
				je.Seq = value.Int64
			}
		case jobevent.FieldJobID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field job_id", values[i])
			} else if value.Valid {
				je.JobID = value.String
			}
		case jobevent.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				je.Type = value.String
			}
		case jobevent.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &je.Payload); err != nil {
					return fmt.Errorf("unmarshal field payload: %w", err)
				}
			}
		case jobevent.FieldTraceContext:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field trace_context", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &je.TraceContext); err != nil {
					return fmt.Errorf("unmarshal field trace_context: %w", err)
				}
			}
		case jobevent.FieldPostedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field posted_by", values[i])
			} else if value.Valid {
				je.PostedBy = new(string)
				*je.PostedBy = value.String
			}
		case jobevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				je.CreatedAt = value.Time
			}
		default:
			je.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JobEvent.
// This includes values selected through modifiers, order, etc.
func (je *JobEvent) Value(name string) (ent.Value, error) {
	return je.selectValues.Get(name)
}

// Update returns a builder for updating this JobEvent.
// Note that you need to call JobEvent.Unwrap() before calling this method if this JobEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (je *JobEvent) Update() *JobEventUpdateOne {
	return NewJobEventClient(je.config).UpdateOne(je)
}

// Unwrap unwraps the JobEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (je *JobEvent) Unwrap() *JobEvent {
	_tx, ok := je.config.driver.(*txDriver)
	if !ok {
		panic("ent: JobEvent is not a transactional entity")
	}
	je.config.driver = _tx.drv
	return je
}

// String implements the fmt.Stringer.
func (je *JobEvent) String() string {
	var builder strings.Builder
	builder.WriteString("JobEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", je.ID))
	builder.WriteString("seq=")
	builder.WriteString(fmt.Sprintf("%v", je.Seq))
	builder.WriteString(", ")
	builder.WriteString("job_id=")
	builder.WriteString(je.JobID)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(je.Type)
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", je.Payload))
	builder.WriteString(", ")
	builder.WriteString("trace_context=")
	builder.WriteString(fmt.Sprintf("%v", je.TraceContext))
	builder.WriteString(", ")
	if v := je.PostedBy; v != nil {
		builder.WriteString("posted_by=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(je.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// JobEvents is a parsable slice of JobEvent.
type JobEvents []*JobEvent
