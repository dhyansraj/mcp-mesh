// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"mcp-mesh/src/core/ent/schemaentry"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// SchemaEntry is the model entity for the SchemaEntry schema.
type SchemaEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Content hash like 'sha256:abc...'. Primary identity.
	Hash string `json:"hash,omitempty"`
	// Canonical normalized JSON Schema (post-normalizer output).
	Canonical map[string]interface{} `json:"canonical,omitempty"`
	// Runtime that originally produced the raw schema. Informational.
	RuntimeOrigin schemaentry.RuntimeOrigin `json:"runtime_origin,omitempty"`
	// First time this canonical schema was seen by the registry.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SchemaEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case schemaentry.FieldCanonical:
			values[i] = new([]byte)
		case schemaentry.FieldID:
			values[i] = new(sql.NullInt64)
		case schemaentry.FieldHash, schemaentry.FieldRuntimeOrigin:
			values[i] = new(sql.NullString)
		case schemaentry.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SchemaEntry fields.
func (se *SchemaEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case schemaentry.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			se.ID = int(value.Int64)
		case schemaentry.FieldHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field hash", values[i])
			} else if value.Valid {
				se.Hash = value.String
			}
		case schemaentry.FieldCanonical:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field canonical", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &se.Canonical); err != nil {
					return fmt.Errorf("unmarshal field canonical: %w", err)
				}
			}
		case schemaentry.FieldRuntimeOrigin:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field runtime_origin", values[i])
			} else if value.Valid {
				se.RuntimeOrigin = schemaentry.RuntimeOrigin(value.String)
			}
		case schemaentry.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				se.CreatedAt = value.Time
			}
		default:
			se.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SchemaEntry.
// This includes values selected through modifiers, order, etc.
func (se *SchemaEntry) Value(name string) (ent.Value, error) {
	return se.selectValues.Get(name)
}

// Update returns a builder for updating this SchemaEntry.
// Note that you need to call SchemaEntry.Unwrap() before calling this method if this SchemaEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (se *SchemaEntry) Update() *SchemaEntryUpdateOne {
	return NewSchemaEntryClient(se.config).UpdateOne(se)
}

// Unwrap unwraps the SchemaEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (se *SchemaEntry) Unwrap() *SchemaEntry {
	_tx, ok := se.config.driver.(*txDriver)
	if !ok {
		panic("ent: SchemaEntry is not a transactional entity")
	}
	se.config.driver = _tx.drv
	return se
}

// String implements the fmt.Stringer.
func (se *SchemaEntry) String() string {
	var builder strings.Builder
	builder.WriteString("SchemaEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", se.ID))
	builder.WriteString("hash=")
	builder.WriteString(se.Hash)
	builder.WriteString(", ")
	builder.WriteString("canonical=")
	builder.WriteString(fmt.Sprintf("%v", se.Canonical))
	builder.WriteString(", ")
	builder.WriteString("runtime_origin=")
	builder.WriteString(fmt.Sprintf("%v", se.RuntimeOrigin))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(se.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SchemaEntries is a parsable slice of SchemaEntry.
type SchemaEntries []*SchemaEntry
