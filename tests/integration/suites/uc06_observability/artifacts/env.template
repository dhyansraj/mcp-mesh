# Enable distributed tracing
MCP_MESH_TRACING=true

# Redis URL for trace publishing (agents publish here)
MCP_MESH_REDIS_URL=redis://localhost:6379

# Tempo endpoints (for registry)
TEMPO_URL=http://localhost:3200
TELEMETRY_ENDPOINT=localhost:4317
TELEMETRY_PROTOCOL=grpc

# Trace exporter type
TRACE_EXPORTER_TYPE=otlp
MCP_MESH_DISTRIBUTED_TRACING_ENABLED=true
