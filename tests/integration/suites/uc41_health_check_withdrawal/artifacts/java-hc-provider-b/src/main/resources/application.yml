# uc41 survivor provider the consumer fails over to (issue #1480)

spring:
  application:
    name: java-hc-provider-b

server:
  port: ${MCP_MESH_HTTP_PORT:3432}

mesh:
  registry:
    url: ${MCP_MESH_REGISTRY_URL:http://localhost:8000}
  agent:
    name: ${MCP_MESH_AGENT_NAME:hc-provider-b-java}
    namespace: ${MCP_MESH_NAMESPACE:default}

logging:
  level:
    io.mcpmesh: DEBUG
    com.example: DEBUG
